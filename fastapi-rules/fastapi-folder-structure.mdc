---
description: Defines the recommended folder structure for FastAPI projects to maintain organization and separation of concerns within the 'app' directory.
globs: app/**/*.*
alwaysApply: false
---
- Use this basic structure with furrther modificatiuons as per the application requirements

app/
  main.py
  models/
  schemas/
  routers/
  dependencies/
  services/
  tests/
